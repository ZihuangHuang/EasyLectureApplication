package use_case.transcript;

/**
 * Execute the commonTranscriptFactory Use Case
 */
public interface TranscriptInputBoundary {

    /**
     * Execute the commonTranscriptFactory Use Case
     */
    void execute(TranscriptInputData transcriptInputData);
}
